<%= form_with(model: gift, local: true) do |form| %>
  <% if gift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gift.errors.count, "error") %> prohibited this gift from being saved:</h2>

      <ul>
      <% gift.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_gift %>
    <%= form.text_field :name_gift, id: :gift_name_gift %>
  </div>

  <div class="field">
    <%= form.label :link_gift %>
    <%= form.text_field :link_gift, id: :gift_link_gift %>
  </div>

  <div class="field">
    <%= form.label :price_gift %>
    <%= form.number_field :price_gift, id: :gift_price_gift %>
  </div>

   <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, id: :gift_image_gift %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
